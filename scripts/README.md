# Scripts

Place local developer and automation scripts here.

## Available Scripts

- `validate-framework.ps1`: checks that core AI OS markdown files include required sections and that key standards are not placeholder-sized.

Run from the repository root:

```powershell
.\scripts\validate-framework.ps1
```
